#include "../Include/Renderer.h"

namespace RENDER {
    glm::vec3 finalGather(glm::vec3 point, std::vector<Triangle>& triangles, photonmap::PhotonMapper& map, PhotonInfoWrapper photons){
        //Project rays in random hemisphere and sample intersections (can use getRadianceEstimate, or deeper recursion)
        
    }
}
